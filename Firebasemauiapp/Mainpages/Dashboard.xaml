<?xml version="1.0" encoding="utf-8" ?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sfchart="clr-namespace:Syncfusion.Maui.Charts;assembly=Syncfusion.Maui.Charts"
             x:Class="Firebasemauiapp.Mainpages.Dashboard"
             Title="Dashboard">
    <Grid RowDefinitions="Auto,Auto,*,Auto" Padding="16">
        <!-- Header -->
        <Grid Grid.Row="0" ColumnDefinitions="*,Auto" Padding="0,0,0,8">
            <Label Text="Hey Dawn!" FontSize="22" FontAttributes="Bold" TextColor="#3A3A3A" />
            <Button Text="{Binding TodayString}" FontSize="14" BackgroundColor="#F2F2F2" TextColor="#3A3A3A" CornerRadius="12" Padding="10,2" />
        </Grid>

        <!-- Mood Card -->
        <Border Grid.Row="1" BackgroundColor="#F8FAED" Stroke="#E0E0E0" StrokeThickness="1" StrokeShape="RoundRectangle 18" Padding="0" Margin="0,0,0,8">
            <VerticalStackLayout Spacing="0">
                <Grid RowDefinitions="Auto,Auto,Auto,Auto" Padding="0,0,0,0">
                    <!-- Mood Image -->
                    <Image Source="sadness.png" HeightRequest="110" WidthRequest="110" HorizontalOptions="Center" Margin="0,12,0,0" Grid.Row="0" />
                    <!-- Day Selector -->
                    <HorizontalStackLayout Spacing="10" Margin="0,8,0,0" HorizontalOptions="Center" Grid.Row="1">
                        <Button Text="Sun" FontSize="13" BackgroundColor="#50A65D" TextColor="White" CornerRadius="16" WidthRequest="36" HeightRequest="36" />
                        <Button Text="Mon" FontSize="13" BackgroundColor="#E0E0E0" TextColor="#3A3A3A" CornerRadius="16" WidthRequest="36" HeightRequest="36" />
                        <Button Text="Tue" FontSize="13" BackgroundColor="#E0E0E0" TextColor="#3A3A3A" CornerRadius="16" WidthRequest="36" HeightRequest="36" />
                        <Button Text="Wed" FontSize="13" BackgroundColor="#E0E0E0" TextColor="#3A3A3A" CornerRadius="16" WidthRequest="36" HeightRequest="36" />
                        <Button Text="Thu" FontSize="13" BackgroundColor="#E0E0E0" TextColor="#3A3A3A" CornerRadius="16" WidthRequest="36" HeightRequest="36" />
                        <Button Text="Fri" FontSize="13" BackgroundColor="#E0E0E0" TextColor="#3A3A3A" CornerRadius="16" WidthRequest="36" HeightRequest="36" />
                        <Button Text="Sat" FontSize="13" BackgroundColor="#E0E0E0" TextColor="#3A3A3A" CornerRadius="16" WidthRequest="36" HeightRequest="36" />
                    </HorizontalStackLayout>
                    <!-- Caption -->
                    <Label Text="Grow your tree as you grow your mind!" FontSize="13" TextColor="#3A3A3A" HorizontalOptions="Center" Margin="0,8,0,0" Grid.Row="2"/>
                </Grid>
                <!-- More mood tracking history -->
                <Button Text="More mood tracking history"
                        FontSize="14"
                        BackgroundColor="#E6F4EA"
                        TextColor="#50A65D"
                        CornerRadius="10"
                        Margin="12,8,12,12"
                        HorizontalOptions="Fill"
                        Grid.Row="3"
                        Command="{Binding GoToDiaryHistoryCommand}"/>
            </VerticalStackLayout>
        </Border>

        <!-- Mental Wellness Score Chart -->
        <Border Grid.Row="2" BackgroundColor="#E6F4EA" Stroke="#E0E0E0" StrokeThickness="1" StrokeShape="RoundRectangle 18" Padding="0">
            <VerticalStackLayout Spacing="0" Padding="12,8,12,12">
                <Label Text="Mental Wellness Score" FontSize="16" FontAttributes="Bold" TextColor="#50A65D" Margin="0,0,0,8" />
                <!-- Syncfusion Line Chart Centered -->
                <Grid VerticalOptions="Center" HorizontalOptions="Center">
                    <sfchart:SfCartesianChart HeightRequest="180" WidthRequest="320" VerticalOptions="Center" HorizontalOptions="Center">
                        <sfchart:SfCartesianChart.XAxes>
                            <sfchart:CategoryAxis />
                        </sfchart:SfCartesianChart.XAxes>
                        <sfchart:SfCartesianChart.YAxes>
                            <sfchart:NumericalAxis Maximum="10" Minimum="0" />
                        </sfchart:SfCartesianChart.YAxes>
                        <sfchart:LineSeries
                            ItemsSource="{Binding ChartData}"
                            XBindingPath="Day"
                            YBindingPath="Score"/>
                    </sfchart:SfCartesianChart>
                </Grid>
            </VerticalStackLayout>
        </Border>
    </Grid>
</ContentPage>