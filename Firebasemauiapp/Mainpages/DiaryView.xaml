<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="Firebasemauiapp.Mainpages.DiaryView"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:conv="clr-namespace:Firebasemauiapp.Converters"
             Title="DiaryView"
             BackgroundColor="#FEAA3A"
             Shell.NavBarIsVisible="False">
    <ContentPage.Resources>
        <conv:MoodNameToImageConverter x:Key="MoodNameToImageConverter"/>
        <conv:ImageUrlToHeightConverter x:Key="ImageUrlToHeightConverter"/>
    </ContentPage.Resources>
    <Grid RowDefinitions="Auto,*"
          Padding="20"
          RowSpacing="12">
        <!-- Header -->
        <Grid Grid.Row="0"
              RowDefinitions="Auto,Auto"
              ColumnDefinitions="Auto,*"
              Margin="0,0,0,12">
            <ImageButton Source="back.png"
                         Clicked="OnBackClicked"
                         BackgroundColor="Transparent"
                         WidthRequest="80"
                         HeightRequest="80"/>
            <Image Source="calendar.png"
                   Grid.Row="1"
                   Grid.Column="0"
                   WidthRequest="28"
                   HeightRequest="28"
                   Margin="0,4,8,0"/>
            <Label x:Name="DateLabel"
                   Grid.Column="1"
                   Grid.Row="1"
                   TextColor="White"
                   FontSize="20"
                   Opacity="0.9"
                   Margin="0,4,0,0"/>
        </Grid>
        <!-- Content with AbsoluteLayout for precise positioning -->
        <AbsoluteLayout Grid.Row="1">
            <!-- Mood indicator - Fixed at top -->
            <Border BackgroundColor="White"
                    WidthRequest="70"
                    HeightRequest="70"
                    AbsoluteLayout.LayoutBounds="0, 0, 70, 70"
                    AbsoluteLayout.LayoutFlags="None">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="12"/>
                </Border.StrokeShape>
                <Image Source="{Binding Mood.Name, Converter={StaticResource MoodNameToImageConverter}}"
                       Margin="8"
                       Aspect="AspectFit"/>
            </Border>

            <!-- Diary editor - expanded to fill more space -->
            <Border x:Name="EditorBorder"
                    StrokeThickness="0"
                    BackgroundColor="Transparent"
                    Padding="10"
                    AbsoluteLayout.LayoutBounds="0, 58, 1, 420"
                    AbsoluteLayout.LayoutFlags="WidthProportional">
                <Editor x:Name="DiaryEntry"
                        BackgroundColor="Transparent"
                        Placeholder="share something good..."
                        Text="{Binding DiaryContent}"
                        FontSize="16"
                        TextColor="White"
                        VerticalOptions="FillAndExpand"
                        VerticalTextAlignment="Start"/>
            </Border>

            <!-- White Border Container for Image Area -->
            <Border x:Name="ImageBorder"
                    BackgroundColor="White"
                    StrokeThickness="0"
                    AbsoluteLayout.LayoutBounds="0, 488, 1, 150"
                    AbsoluteLayout.LayoutFlags="WidthProportional">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="20"/>
                </Border.StrokeShape>
                <Grid>
                    <!-- Upload Image Button - Large circular green button in center (shown when no image) -->
                    <Border BackgroundColor="#A8D5BA"
                            WidthRequest="180"
                            HeightRequest="180"
                            HorizontalOptions="Center"
                            VerticalOptions="Center">
                        <Border.Triggers>
                            <DataTrigger TargetType="Border"
                                         Binding="{Binding ImageUrl}"
                                         Value="">
                                <Setter Property="IsVisible"
                                        Value="True"/>
                            </DataTrigger>
                            <DataTrigger TargetType="Border"
                                         Binding="{Binding ImageUrl}"
                                         Value="{x:Null}">
                                <Setter Property="IsVisible"
                                        Value="True"/>
                            </DataTrigger>
                        </Border.Triggers>
                        <Border.StrokeShape>
                            <Ellipse/>
                        </Border.StrokeShape>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding UploadImageCommand}"/>
                        </Border.GestureRecognizers>
                        <VerticalStackLayout VerticalOptions="Center"
                                             HorizontalOptions="Center"
                                             Spacing="8">
                            <Image Source="imgicon.png"
                                   WidthRequest="48"
                                   HeightRequest="48"
                                   HorizontalOptions="Center"/>
                            <Label Text="May I image (png,only)*"
                                   FontSize="12"
                                   TextColor="White"
                                   HorizontalOptions="Center"
                                   HorizontalTextAlignment="Center"/>
                        </VerticalStackLayout>
                    </Border>

                    <!-- Uploaded Image (shown when image exists) -->
                    <Image Source="{Binding ImageUrl}"
                           Aspect="AspectFill"
                           HorizontalOptions="Fill"
                           VerticalOptions="Fill">
                        <Image.Clip>
                            <RoundRectangleGeometry CornerRadius="20"
                                                    Rect="0,0,1000,1000"/>
                        </Image.Clip>
                        <Image.Triggers>
                            <DataTrigger TargetType="Image"
                                         Binding="{Binding ImageUrl}"
                                         Value="">
                                <Setter Property="IsVisible"
                                        Value="False"/>
                            </DataTrigger>
                            <DataTrigger TargetType="Image"
                                         Binding="{Binding ImageUrl}"
                                         Value="{x:Null}">
                                <Setter Property="IsVisible"
                                        Value="False"/>
                            </DataTrigger>
                        </Image.Triggers>
                    </Image>
                </Grid>
            </Border>

            <!-- Action buttons at bottom -->
            <Grid ColumnDefinitions="Auto,*"
                  ColumnSpacing="12"
                  AbsoluteLayout.LayoutBounds="0, 640, 1, 70"
                  AbsoluteLayout.LayoutFlags="WidthProportional"
                  Margin="0,0,0,10">
                <!-- Cancel/Close Button -->
                <Border Grid.Column="0"
                        BackgroundColor="White"
                        WidthRequest="56"
                        HeightRequest="56"
                        VerticalOptions="Center">
                    <Border.StrokeShape>
                        <Ellipse/>
                    </Border.StrokeShape>
                    <Border.Shadow>
                        <Shadow Brush="#40000000"
                                Radius="12"
                                Offset="0,4"
                                Opacity="0.35"/>
                    </Border.Shadow>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnCancelClicked"/>
                    </Border.GestureRecognizers>
                    <Label Text="✕"
                           FontSize="24"
                           TextColor="#FEAA3A"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"/>
                </Border>

                <!-- Next Button -->
                <Button Grid.Column="1"
                        x:Name="AnalyzeButton"
                        BackgroundColor="White"
                        TextColor="#FEAA3A"
                        Text="Next"
                        Command="{Binding AnalyzeContentCommand}"
                        IsEnabled="{Binding IsAnalyzing, Converter={StaticResource InvertedBoolConverter}}"
                        CornerRadius="28"
                        HeightRequest="56"
                        HorizontalOptions="Fill"
                        VerticalOptions="Center"
                        FontAttributes="Bold"
                        FontSize="18">
                    <Button.Shadow>
                        <Shadow Brush="#40000000"
                                Radius="16"
                                Offset="0,4"
                                Opacity="0.35"/>
                    </Button.Shadow>
                </Button>
            </Grid>

            <!-- Loading indicator -->
            <ActivityIndicator Margin="10"
                               Color="White"
                               IsRunning="{Binding IsLoadingVisible}"
                               IsVisible="{Binding IsLoadingVisible}"
                               AbsoluteLayout.LayoutBounds="0.5, 0.5, 50, 50"
                               AbsoluteLayout.LayoutFlags="PositionProportional"/>

            <!-- Upload Activity Indicator -->
            <ActivityIndicator Color="#50A65D"
                               IsRunning="{Binding IsUploadingImage}"
                               IsVisible="{Binding IsUploadingImage}"
                               AbsoluteLayout.LayoutBounds="0.5, 360, 50, 50"
                               AbsoluteLayout.LayoutFlags="XProportional"/>
        </AbsoluteLayout>
    </Grid>
</ContentPage>