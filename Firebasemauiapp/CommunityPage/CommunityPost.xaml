<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
     xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
     xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
     x:Class="Firebasemauiapp.CommunityPage.CommunityPost"
     x:Name="RootPopup"
     CanBeDismissedByTappingOutsideOfPopup="True"
     BackgroundColor="Transparent"
     Padding="0">
    <!-- AbsoluteLayout to avoid Grid measurement artifacts that showed white lines -->
        <Grid HorizontalOptions="Center"
         VerticalOptions="Center"
         Padding="0"
         BackgroundColor="Transparent">
       <!-- Background image fills intrinsic size -->
       <Image Source="Commupost"
         Aspect="Fill"
         HorizontalOptions="Center"
         VerticalOptions="Center" />
   
       <!-- Overlay layer -->
       <Grid Padding="20"
        BackgroundColor="Transparent">
           <!-- Close button top-right -->
           <Button Text="✕"
              HorizontalOptions="End"
              VerticalOptions="Start"
              BackgroundColor="Transparent"
              TextColor="Black"
              FontSize="24"
              Padding="0"
              Clicked="OnCloseClicked" />
   
           <!-- Content -->
           <ScrollView IsVisible="{Binding HasPost}"
             HorizontalOptions="Center"
             VerticalOptions="Center"
             Margin="0,50,0,60">
          <Label Text="{Binding RandomPost.Content}"
            FontSize="14"
            TextColor="#222"
            HorizontalTextAlignment="Center" />
           </ScrollView>
           <ContentView IsVisible="{Binding NoPost}"
              HorizontalOptions="Center"
              VerticalOptions="Center"
              Margin="0,50,0,60">
          <Label Text="ไม่มีโพสต์ในขณะนี้"
            FontSize="14"
            TextColor="#555"
            HorizontalTextAlignment="Center" />
           </ContentView>
   
           <!-- Like section bottom-right -->
           <Grid ColumnDefinitions="Auto,Auto"
            ColumnSpacing="4"
            HorizontalOptions="End"
            VerticalOptions="End">
          <Label Text="{Binding RandomPost.Likes}"
            VerticalOptions="Center"
            FontSize="14"
            FontAttributes="Bold"
            TextColor="#222" />
          <Button Text="👍"
             BackgroundColor="Transparent"
             FontSize="28"
             Padding="0"
             Clicked="OnLikeClicked" />
           </Grid>
       </Grid>
        </Grid>
</toolkit:Popup>
